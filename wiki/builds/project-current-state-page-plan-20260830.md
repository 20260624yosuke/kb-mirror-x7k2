---
type: build
status: active
confidence: medium
evidence_level: user-stated
last_reviewed: 2026-08-31
revision: 4
plan_status: draft-unapproved
sources:
  - project-hub-index
  - project-current-state-page-plan-review-20260830
  - project-current-state-page-plan-review2-20260830
related:
  - "[[gf2-helen-repro-execution-audit-plan-20260830]]"
  - "[[gf2-helen-deliverable-unified-route-plan-20260831]]"
---

# プロジェクト現在位置ページの導入 — 実装計画 rev.4（2026-08-31）

> [!warning] rev.4 は「正本の衝突」を解消するための作り直し
> 2026-08-31、**別のセッションが同じ helen 案件で並行して計画を進めていた**ことが判明した。
> 相手の `gf2-helen-repro-execution-audit-plan-20260830.md`（revision 4・**実行承認済み**）は
> 194行目で「**状態値の唯一の正本は `state.json`**」と定めている。
> rev.3 までの本計画は「正本は wiki 側の現在位置ページ1枚」＋ `run-state.common.json` の新設で、
> **正面から衝突していた**。武田さんの判断（2026-08-31）により、**承認済みの相手側へ寄せる**。
>
> 主な変更:
> 1. **`run-state.common.json` の新設をやめた**（4節を削除）。機械入力は `06_repro-v51/audit/state.json`。
> 2. **本計画の範囲を縮小**（2節）。現在位置ページは `state.json` の**表示層**と位置づける。
> 3. **`state.json` への拡張要求**を新設（4節）。相手の計画へ渡す。
> 4. **相手の P0B 完了待ちになる**ことを明示（7節）。`06_repro-v51/audit/` は現時点で未作成。
> 5. 3回目レビューの指摘を反映（1.3 の対象範囲・判定④の書式・判定①の強制点・節1/5 の収集手順・
>    LLM 区画の保全試験・9.2 の矛盾・11節-4 の事実誤り）。

> [!info] この計画の位置づけ
> ブレストメモの正本:
> `/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01/wiki/analyses/brainstorm/project-hub-index/_index.md`
> **この計画はまだ実装承認を得ていない。**

---

## 0. 用語

- **KB ルート**: `/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01`
- **現在位置ページ**: 1案件につき1枚。新しいセッションが最初に読む唯一の入口。**表示層**であり、状態は持たない。
- **`state.json`**: `06_repro-v51/audit/state.json`。相手の承認済み計画が定める**状態の唯一の正本**。
  更新は相手の `audit_guard.py` だけが行う。**本計画は読むだけ。**
- **`run-state.json`**: 既存の記録ファイル。**本計画では読み取り専用。履歴として扱う。**
- **機械区画 / LLM 区画**: 現在位置ページ内で、スクリプトが書く節と LLM が会話から書き起こす節。

---

## 1. 解こうとしている問題（実測・2026-08-30〜31）

セッションが切り替わるたびに、武田さんが「引き継ぎ資料を作って」と LLM へ指示し、
出てきた資料を次のセッションへ添付している。これが次の3つを生んでいる。

1. **毎回の手間**が発生する。
2. **その資料がどれだけ保持できているか測れない。**
3. **資料が増える。** 1.2 の7枚・合計4,593行。

### 1.1 構造的な壊れ（4件）

| # | 壊れ | 実測値 | rev.4 での担当 |
| --- | --- | --- | --- |
| 1 | 現在位置を主張するものが複数系統 | `run-state.json` 2026-08-26 20:55 ／ wiki 側 08-27〜08-30 ／ **相手の `state.json`（未作成）** | 相手の計画（`state.json` を唯一の正本にする） |
| 2 | 閉じた項目が「残り」に残る | `agent_executable_remaining` は3件を名乗るが、実行可能な残りは0件 | 相手の計画（finding の closure 規定） |
| 3 | 派生案件の所在がバラバラ | 水着化だけ KB 内 `output/gf2-helen-swimsuit/` | **本計画**（節6・承認D） |
| 4 | 引き継ぎ資料が互いに「自分が正本」と本文で宣言 | 1.3 のとおり。**4〜6行では足りず、実測で10か所以上** | **本計画**（1.3・案1） |

### 1.2 引き継ぎ資料7枚（2026-08-31 に `wc -l` で実測。3回目レビューでも一致を確認）

| # | 実パス | 行数 |
| --- | --- | --- |
| 1 | `wiki/builds/gf2-helen-repro-v51-run.md` | 1737 |
| 2 | `wiki/builds/gf2-helen-repro-v51-handoff.md` | 1582 |
| 3 | `wiki/builds/gf2-helen-repro-plan-repair-model-routing-handoff-20260827.md` | 585 |
| 4 | `wiki/builds/gf2-repro-and-swimsuit-conversation-handoff-20260827.md` | 235 |
| 5 | `06_repro-v51/reports/HANDOFF.md` | 313 |
| 6 | `06_repro-v51/reports/NEXT-SESSION-PROMPT.md` | 131 |
| 7 | `06_repro-v51/reports/HANDOFF-2026-08-20.md` | 10 |

合計 **4,593行**。実装の手順1で再計測し、値が違えば更新してから進む。

### 1.3 壊れ#4 の実態と、武田さんの決定（案1）

**rev.3 は対象を「4〜6行」と固定していたが、3回目レビューの実測で少なくとも10か所以上あることが判明した。**
また、行ごと置換すると**文が途中で切れて壊れる**箇所がある（run.md は12〜14行で1文、
conversation は26〜27行で1文、plan-repair は54〜55行で1文）。rev.4 で次のように直す。

- **対象は「行」ではなく「現在位置を主張している文または語句」**とする。
  置換単位は文または語句であり、行ではない。
- **対象は手順3で全件を機械的に洗い出す**（`正本` / `こちらが新しい` / `現在位置` / `最新` /
  `正規入口` を含む箇所を全ファイルから抽出し、1件ずつ人が読んで「現在位置の主張か否か」を仕分ける）。
  **4〜6件に固定しない。**
- 判明済みの例（2026-08-30〜31 実読）:

| ファイル | 行 | 文面（抜粋） |
| --- | --- | --- |
| `gf2-helen-repro-v51-run.md` | 12〜14 | 「…に従う実行の**正本**。この会話の記憶ではなく…」 |
| `gf2-helen-repro-v51-run.md` | 122 | 「会話履歴ではなく**このページが正本**。」 |
| `gf2-helen-repro-v51-handoff.md` | 17 / 30 / 31 / 55 | 「以後の更新は**ここへ直接書く**」「**衝突したらこちらが新しい**」ほか |
| `gf2-helen-repro-plan-repair-…-20260827.md` | 54〜55 | 「section 9 を**正本とする**」（同一ページ内の節指定。仕分け対象） |
| `gf2-repro-and-swimsuit-conversation-…md` | 26〜27 | 「巨大な既存**正本**を置き換えず」（**主張ではなく断り書き**。仕分けで除外しうる） |
| `reports/NEXT-SESSION-PROMPT.md` | 2 / 12 / 16 | 「現在の正本は…」「**現在位置を持ちます**」「**現在位置はここが最新**」 |
| `reports/HANDOFF.md` | 20 / 51 | 「**現在位置** ＝ `06_repro-v51/run-state.json`」ほか |
| `reports/HANDOFF-2026-08-20.md` | 5 | 「**新しい所在（正本）**: …」 |
| `index.md` | 1429 / 1435 | 「実行記録（**正本**）」「別セッションで再開する**正規入口**」 |

**武田さんの承認（2026-08-30・案1）**: 現在位置を主張している箇所**だけ**を
「このページは旧。現在位置は○○が正本」へ直す。**それ以外の本文は触らない。**
変更前後の全文を記録に残す。

- 記録先: `wiki/builds/gf2-helen-master-claim-rewrites-20260831.md`（新規）。
- **これは禁止事項「既存の本文を書き換えない」の唯一の例外**（10節-2）。
- **仕分け結果と置換案の全件を、承認B-2 で武田さんに見せてから実行する。**

### 1.4 分量の問題

`run-state.json` は 129,913文字・履歴113件。現在位置ページの 400行上限（3.4）で対処する。

### 1.5 承認済みの要件「案1と案2を両方」

- **案1** = 現在位置ページを新設する。
- **案2** = 既存ページ側から現在位置ページへ**戻る道**を作る。置くのは**1行ポインタだけ**。

---

## 2. 完成条件（rev.4 で縮小）

**合格の基準は1つ。「引き継ぎ資料を作って」と指示せずに、新しいセッションが HELEN-REPRO v5.1 を再開できること。**
判定方法は9.2。以下はその内訳。

1. `wiki/builds/` に **1案件1枚の現在位置ページ**が存在する（新ディレクトリ・新 `type` は作らない）。
2. ページが **8見出し構成**で、機械区画と LLM 区画がマーカーで分かれている（3.3）。
3. **機械区画が `state.json` から生成されている。** 本計画は `state.json` を書かない。
4. **5つの機械判定**が動く（5節）。フック登録と実効性確認を、9.2 の本番試験**より前**に終える。
5. **壊れ#4 が解消されている。** 判定基準は
   「1.3 の仕分けで**現在位置の主張と判定された全件**が書き換わり、記録ファイルに変更前後が全件残り、
   `check` が残存ゼロを機械で確認できること」。
6. **既存ページ側から現在位置ページへ戻れる**（案2）。対象は 1.2 の7枚と `index.md` の該当行。

### 完成条件に**含まない**もの

- **`state.json` の実装**（相手の計画・P0B の担当）。
- 成果物 blend の変更。helen の工程を進めること。
- 壊れ#1・#2 の解消（相手の計画の担当。1.1 の表を参照）。
- helen 以外3案件（承認D）。

---

## 3. 現在位置ページの仕様

### 3.1 置き場と命名

- `wiki/builds/<案件slug>-current-state.md`（helen → `gf2-helen-repro-current-state.md`）
- **新ディレクトリを作らない・新 `type` を作らない。**

> [!warning] frontmatter の欄追加は KB の構造決定にあたる（`CLAUDE.md` 2節）。承認A-1 の対象。

### 3.2 frontmatter

```yaml
---
type: build
status: active
confidence: high
evidence_level: source-backed
last_reviewed: YYYY-MM-DD
sources: []

current_state_page: true
project_slug: gf2-helen-repro
project_status: active            # active | blocked | paused | done
work_roots:
  - /絶対パス
state_file: /絶対パス/06_repro-v51/audit/state.json
synced_from:
  "/絶対パス/06_repro-v51/audit/state.json": "YYYY-MM-DDTHH:MM:SS+09:00"
last_synced: "YYYY-MM-DDTHH:MM:SS+09:00"
---
```

- `last_synced` / `synced_from` はスクリプトだけが書く。

### 3.3 本文の見出し（8つ・完全一致で固定）

| # | 見出し | 主体 | 生成元 |
| --- | --- | --- | --- |
| 1 | `## いま何をしているか` | **LLM** | 会話から書き起こす。3行以内 |
| 2 | `## 現在位置` | 機械 | `state.json` の defect ごとの状態 |
| 3 | `## 止まっている理由` | 機械 | `state.json` の `blocked` / `contested` と `reason_id` |
| 4 | `## 次の選択肢` | 機械 | `state.json` の拡張欄（4節の要求1） |
| 5 | `## 武田さんの判断` | **LLM** | 会話から書き起こす。書式は 3.5 |
| 6 | `## 関連ファイル（実パス）` | 機械 | `state.json` の拡張欄（4節の要求2） |
| 7 | `## 履歴の所在` | 機械 | `state.json` の拡張欄（4節の要求3） |
| 8 | `## 片付いた記録` | 機械 | `state.json` の `accepted` / `rejected` と closed finding |

- **LLM 区画は節1と節5だけ。**
- **機械区画はマーカーで囲む**（3回目レビュー §2-7 への対処）。

```markdown
<!-- machine-section:start name="現在位置" -->
（機械が置換する範囲）
<!-- machine-section:end name="現在位置" -->
```

- `sync` は**マーカーの内側だけ**を置換する。マーカーが無ければ止める（節1・節5 を壊さないため）。

### 3.4 分量の上限

- **400行以内。** 超えても `sync` をエラーにしない。次の順に切り詰める。
  1. 節8を `<slug>-current-state-archive.md` へ移す（3.6）
  2. 節7の列挙を件数だけに縮める
  3. それでも超えたら超過した旨を節7の末尾に1行書いて生成する（警告）
- **切り詰めが起きた場合、`verify` の照合はマーカー内の残存分だけを対象にする**
  （3回目レビュー §2-2 への対処。切り詰めで verify が恒常的に落ちるのを防ぐ）。
- 400行に根拠は無い。helen で実測してから調整する。

### 3.5 節5「武田さんの判断」の書式（判定④のため固定）

```markdown
| 日付 | 状態 | 判断 | 出どころ |
| --- | --- | --- | --- |
| 2026-08-18 | 有効 | 胸下のイボイボは現状問題なし。この項目は閉じる | 会話（武田さん明示） |
```

- `状態` は `有効` / `失効` のみ。`失効` の行は消さずに残す。
- **判定④は、ヘッダ行（`| 日付 |…`）と区切り行（`| --- |…`）を除外したデータ行だけを見る**
  （3回目レビュー §3-2 への対処。rev.3 の素直な実装では常に FAIL した）。
- 節5 が0行のとき（判断がまだ無い）は `（なし）` と1行書く。空節は FAIL。

### 3.6 archive ファイル（3.4-1 で作る場合）

- `wiki/builds/<slug>-current-state-archive.md`。`type: build` / `status: superseded` /
  `project_slug` は同じ。**`current_state_page` は付けない**（判定①に当たらないため）。
- **追記のみ。再生成しない**（3回目レビュー §2-3 への対処。再生成すると過去の完了記録が消える）。
  同じ項目が既にあれば追記しない（項目 ID で重複判定）。
- 冒頭1行: 「これは `<slug>-current-state.md` の片付いた記録の置き場。現在位置ではない。」
- `index.md` へ登録する。案2のポインタは不要。

---

## 4. `state.json` への拡張要求（相手の計画へ渡す）

現在位置ページの節4・6・7 は、相手の承認済み計画の `state.json` には**現時点で無い**。
相手の unified-route 計画は「予定済みの `audit/state.json` を**拡張する**」と述べているため、
拡張自体は方針と矛盾しない。**次の3つを拡張要求として渡し、相手側の承認を得る。**

| # | 要求する欄 | 用途 | 無い場合の代替 |
| --- | --- | --- | --- |
| 1 | `next_choices[]`（`label` / `what` / `cost`） | 節4「次の選択肢」 | 節4を LLM 区画へ戻す（心がけ担保に後退。非推奨） |
| 2 | `related`（`projects` / `handoffs[{path,status}]` / `data`） | 節6「関連ファイル」・案2の `現行/旧` の唯一の出所 | 節6を作れない。案2の `status` 判定も不能 |
| 3 | `history_ref`（`file` / `key` / `count`） | 節7「履歴の所在」 | 節7を件数なしにする |

- **要求2 が最重要。** これが無いと案2（既存ページからの戻り道）の `現行/旧` を機械で決められず、
  二重管理に戻る。
- 拡張要求は `wiki/builds/gf2-helen-state-json-extension-request-20260831.md` に書き、
  相手の計画の担当へ渡す（承認A-2）。
- **相手が拒否した場合**、本計画は節4・6・7 を持てない。その場合は
  「現在位置ページは節1/2/3/5/8 のみ」に縮小し、案2は保留する（11節-1）。

---

## 5. 5つの機械判定

実装先: `<KBルート>/tools/project_state_sync.py`（新規）。

| # | 判定 | 発火点 | 落ちたときの動作 |
| --- | --- | --- | --- |
| ① | 同じ `project_slug` の現在位置ページが2枚以上ないか | `sync` の冒頭・`check`・**`wiki/builds/` への書き込み前（フック）** | エラーで止め、両方のパスを表示 |
| ② | 節2/3/4/6/7/8 をマーカー内へ生成 | `sync` | 入力欄が欠けていれば止める（部分生成しない）。マーカーが無ければ止める |
| ③ | 節5の全データ行に日付があり状態が `有効`/`失効` か | `check` | 該当行を挙げて FAIL |
| ④ | `synced_from[state.json]` と `state.json` の実 mtime のずれ | `work_roots` への書き込み前（フック） | 拒否し `sync` を促す |
| ⑤ | 現在位置ページを読まずに `work_roots` へ書こうとしていないか | 同上 | 拒否し、現在位置ページの実パスを示す |

- **判定①の強制点**（3回目レビュー §3-4 への対処）: フックの適用範囲に `wiki/builds/` を加える。
  `check` を誰かが走らせるまで検出されない状態にしない。
- 判定①の走査範囲: `<KBルート>/wiki/` 配下の `*.md`。`_attachments/` と `*.bak*` は除外。
- **rev.3 の判定⑤b（`run-state.json` とのずれ）は削除した。**
  状態の正本が `state.json` に一本化されるため、`run-state.json` は履歴になり、ずれの概念が消える。

### 5.1 判定④⑤の適用範囲と除外

1. **除外**: `state.json` / `run-state.json` / `*.bak*` / `__pycache__/` /
   `<KBルート>/logs/project-state-sync.log` / **`06_repro-v51/reports/`** への書き込み。
   （`reports/` を除外するのは、正解表の出力先が `work_roots` 内にあり自己ロックするため。
   3回目レビュー §2-4 への対処。）
2. **適用範囲の限定**: `wiki/builds/` に `current_state_page: true` のページが存在し、その
   `work_roots` に含まれるパスだけを対象にする。**該当しなければ即座に通す。**
3. **脱出口**: `resync --force <slug>` で `synced_from` だけを更新できる。
   使用は `<KBルート>/logs/project-state-sync.log` に残す。
   **`logs/` は新規ディレクトリなので、承認A-1 に含める**（3回目レビュー §2-8）。
4. **エラーで詰まない**: 400行超は警告に留め、`sync` は成功する。
5. **外付け未マウント時**: `stat` 失敗なら**止める**（安全側）。マウント案内を出す。
6. **既存フックとの共存**: `~/.claude/settings.json` の PreToolUse には**既に2本**登録されている
   （`brainstorm_guard.py guard-write --unread` / `tools/prose_guard.py`）。**3本目になる。**
   発火順・deny の合成・相互の誤検知は 9.1 の試験13〜15 で確認する。

### 5.2 コマンド

```
python3 tools/project_state_sync.py sync    <slug>          # 機械区画を生成
python3 tools/project_state_sync.py check   <slug>          # 判定①③＋案2ポインタ＋壊れ#4 の残存
python3 tools/project_state_sync.py verify  <slug>          # 8節の照合
python3 tools/project_state_sync.py answer-key <slug>       # 9.2 の正解表を出力
python3 tools/project_state_sync.py resync --force <slug>   # 脱出口
python3 tools/project_state_sync.py list                    # 一覧と鮮度
```

---

## 6. 案2（既存ページ側からの戻り道）

- 対象: `state.json` の `related.handoffs[]`（4節の要求2）に載っている全ファイルと、
  `index.md` の helen 関連行。
- **ブロックは機械が書く。** `status` の出所は `related.handoffs[].status` のみ。

```markdown
<!-- current-state-pointer:start -->
> [!info] この案件の入口
> 現在位置は `wiki/builds/gf2-helen-repro-current-state.md` が正本。このページは経緯の記録。
> 現行/旧: **旧**
<!-- current-state-pointer:end -->
```

- `check` は、①対象全ファイルにブロックがあること ②`現行/旧` が `state.json` の `status` と一致すること
  ③**`related.handoffs[]` が 1.2 の7枚を網羅していること**（3回目レビュー A-5 への対処）を見る。
- `index.md` は1行カタログなので callout を置けない。**該当行の末尾に
  `（入口: wiki/builds/gf2-helen-repro-current-state.md）` を足す**形にする
  （3回目レビュー §3-10 への対処）。対象行は `project_slug` を含む行とする。

---

## 7. 実装手順

> [!warning] 本計画は相手の P0B 完了待ちで始まる
> `06_repro-v51/audit/state.json` は **2026-08-31 時点で未作成**（`audit/` ディレクトリごと無い）。
> 相手の計画の P0B が完了するまで、手順5以降は着手できない。
> **手順0〜4 は先行できる。**

| 手順 | 内容 | 承認関所 | 依存 |
| --- | --- | --- | --- |
| 0 | rev.4 の縮小方針を提示（本計画の範囲・相手との分担） | **承認A-0** | — |
| 1 | 1.2 の7枚を `wc -l` で再計測 | — | — |
| 2 | 現在位置ページの雛形を手で1枚作る＋frontmatter 欄と `logs/` 新設を提示 | **承認A-1** | — |
| 3 | 1.3 の対象を全件抽出して仕分け、置換案（変更前後の全文）を提示 | **承認B-2** | — |
| 4 | `state.json` 拡張要求（4節）を作成し、相手の計画の担当へ渡す | **承認A-2** | — |
| 5 | 節1・節5 の中身を、7枚と `run-state.json` から**収集して書き起こす** | — | 手順3 |
| 6 | `tools/project_state_sync.py` を実装 | — | **相手の P0B** |
| 7 | helen で `sync` → `verify` | — | 手順6 |
| 8 | 1.3 の置換を実行し、記録ファイルへ変更前後を全件保存 | — | 承認B-2 |
| 9 | 案2のポインタを機械で挿入（7枚と `index.md`） | — | 手順7 |
| 10 | 判定①④⑤のフックを登録し、9.1 の試験を全部通す | — | 手順6 |
| 11 | **本番の閉じた試験**（9.2） | **承認C** | 手順10 |
| 12 | `index.md` 更新・`log.md` 追記・成果物 Inbox へ申告 | — | — |
| 13 | 残り3案件へ展開 | **承認D** | 承認C |

- **手順5 は rev.3 に無かった**（3回目レビュー §3-9 への対処）。節1・節5 を誰がどこから書き起こすかの
  手順が存在せず、「武田さんの判断の散逸」という当初の問題に対して収集工程が無かった。
- 承認A-0・A-1・A-2・B-2・C・D では**停止する**。無回答・沈黙・カードを閉じただけは承認ではない。

---

## 8. `verify` の仕様

`verify <slug>` は生成物と入力を照合する。1つでも不一致があれば非ゼロ終了する。

| # | 照合するもの | 方法 |
| --- | --- | --- |
| 1 | マーカー内の各値 | `state.json` の対応値と一致するか（切り詰めが起きた節は残存分のみ対象） |
| 2 | `state.json` に載る全パス | 実在するか |
| 3 | `related.handoffs[]` | 1.2 の7枚を網羅しているか |
| 4 | 節5 | 3.5 の書式を満たすか（データ行のみ） |
| 5 | マーカーの対応 | `start`/`end` が6組そろっているか |

- **照合できないもの**: `state.json` に書かれた状態の内容が実態と合っているか。
  これは相手の `audit_guard.py` の担当であり、本計画では判定しない（11節-2）。
- **8.1 `answer-key`**: 9.2 の正解表を出力する。内容は
  「defect ごとの状態」「`blocked`/`contested` の全件」「`next_choices[]` の全件」。
  出力先は **`<KBルート>/logs/answer-key-<slug>-YYYYMMDD.md`**
  （`work_roots` の外。3回目レビュー §2-4 への対処）。

---

## 9. 検証手順

### 9.1 機械判定の実効性（手順10で全部通す）

| # | 試験 | 合格条件 |
| --- | --- | --- |
| 1 | `state.json` を `touch` して `work_roots` へ書き込む（同期前） | **拒否される**（④） |
| 2 | `sync` 後に同じ書き込みをする | **通る** |
| 3 | `work_roots` の外へ書き込む | **通る** |
| 4 | `state.json` / `run-state.json` 自身へ書き込む | **通る**（自己ロックしない） |
| 5 | `06_repro-v51/reports/` へ書き込む | **通る**（5.1-1 の除外） |
| 6 | 現在位置ページを読まずに `work_roots` へ書き込む | **拒否される**（⑤） |
| 7 | 同じ `project_slug` のページを2枚目作る | **止まる**（⑤の強制点。作った2枚目は削除） |
| 8 | `sync` を実行し、節1・節5 の内容が**変わっていない**ことを確認 | **不変**（§2-7 への対処。最重要） |
| 9 | マーカーを1つ壊して `sync` | **止まる**（部分生成しない） |
| 10 | 節5のデータ行から日付を1つ消して `check` | **FAIL**（③）。試験後に戻す |
| 11 | 節5がヘッダと区切り行だけの状態で `check` | **FAIL しない**（`（なし）` があれば PASS） |
| 12 | 案2ポインタの `現行/旧` を手で書き換えて `check` | **FAIL**（`state.json` と不一致を検出） |
| 13 | **`work_roots` 内のスクリプトを Bash で実行し `logs/` へ書く**（同期済み） | **通る**（実測された誤検知の型） |
| 14 | **`tools/inbox.py` を Bash で実行する** | **通る**（同上） |
| 15 | **既存2本のフックと同時に発火させる**（`wiki/` 内の md を編集） | **3本とも動き、意図しない deny が出ない** |
| 16 | 生成後のページ行数 | 400行以内、または警告が出て生成は成功 |
| 17 | 1.3 の置換後に `check` | **現在位置主張の残存ゼロ**（完成条件5 の機械判定） |

- 試験2〜5・11・13・14・15 が**誤検知（通るべきものが止まる）の検出**にあたる。
- 試験1・13 は**同期状態の前提を明記**した（3回目レビュー §3-5 への対処）。

### 9.2 本番の閉じた試験（承認C）

1. 手順7の `answer-key` で正解表を生成する。
2. **武田さんが正解表を承認する。**
3. 新しいセッションを開き、**現在位置ページ1枚の実パスだけ**を渡す。
4. 「HELEN-REPRO v5.1 を再開して」と指示する。**「引き継ぎ資料を作って」とは言わない。**
5. 述べられた状態・止まっている理由・次の選択肢を、正解表と照合する。
6. **判定するのは武田さん。** LLM の自己申告を根拠にしない（KB CLAUDE.md 1節）。
7. 合格条件: 正解表の「止まっている理由」の全件と「次の選択肢」の全件が、**過不足なく**述べられること。
8. **不合格条件を修正した**（3回目レビュー §2-5 への対処）。rev.3 は
   「`run-state.json` や7枚を読んだら不合格」としていたが、**現在位置ページの節6・節7 がそれらの
   実パスを載せる設計**なので矛盾していた。rev.4 では次に変える。
   - **不合格**: 試験セッションが「引き継ぎ資料を作ってほしい」と要求した場合。
     または現在位置ページの外の情報を読まないと答えられなかった場合
     （＝節2〜4 に答えが載っていなかった証拠。何が足りなかったかを記録する）。
   - 節6・節7 を辿ること自体は**不合格にしない**（そのための節だから）。

---

## 10. 絶対にやってはいけないこと

1. **武田さんが md を直接書く前提の設計にしない。**
2. **既存の本文を書き換えない。** 例外は **1.3 の仕分けで現在位置の主張と判定された箇所だけ**
   （2026-08-30 武田さん承認・案1）。変更前後の全文を記録に残す。
   **仕分け結果は承認B-2 を経る。それ以外の箇所に触ったら違反。**
3. **`state.json` を書かない。** 状態の更新は相手の `audit_guard.py` だけが行う。
4. **既存 `run-state.json` を1バイトも変更しない。**
5. **成果物 blend に触らない。**
6. **文言ルールで担保して「できた」と報告しない。**
   → ただし節1・節5 の鮮度は現状どの判定でも測れない。**11節-3 に未解決として明記する。**
7. **新ディレクトリ・新 `type` を勝手に作らない。** `logs/` の新設も承認A-1 を経る。
8. **相手の承認済み計画と競合する仕組みを作らない。** 迷ったら相手の計画に合わせる。
9. 承認A-0・A-1・A-2・B-2・C・D を飛ばさない。

---

## 11. 未確認・リスク

1. **`state.json` の拡張要求（4節）が通るかは未確認。** 通らなければ節4・6・7 を作れず、
   案2も保留になる。**本計画の価値の大部分がこの要求に依存している。**
2. **`state.json` に書かれた状態の内容が実態と合っているかは、本計画では判定しない。**
   相手の `audit_guard.py` に依存する。
3. **節1・節5（LLM 区画）の鮮度を測る判定が無い。** 10節-6 と正面から矛盾する。
   古い記述が残っていても機械は気づかない。次の改訂の課題。
4. **判定⑤（未読ブロック）は完全ではない。** 読んだうえで `run-state.json` も読めば古い記述は目に入る。
5. **判定④⑤のフックが実際に発火するかは未確認。** PreToolUse には**既に2本**登録済みで、
   3本目になる（実測 2026-08-31）。発火順・deny の合成は 9.1 試験15 で確認する。
6. **判定④⑤の「読んだか」判定がセッション単位で、サブエージェント／別 Bash セッションでの
   扱いが未確認。** helen の主作業は `scripts/` 258本を Bash で回す形なので、外すと運用が止まるか
   逆に素通りする（3回目レビュー §6-8）。
7. **相手の P0B が完了しないと手順6以降に進めない。** `06_repro-v51/audit/` は未作成。
   相手の進捗が本計画の日程を決める。
8. **相手のセッションと同時に同じファイルを触る危険。** 2026-08-30〜31 に実際に並行作業が起きた。
   `wiki/builds/` と `tools/` は両者が書く場所。着手前に相手の作業状況を確認する。
9. **400行の上限に根拠は無い。**
10. **外付け未マウント時は判定④が止める設計。** 意図した挙動。
11. **絶対パスがボリューム名に固定される。** マウント名が変わると全案件が一斉に壊れる。
12. **現在位置ページが8枚目の引き継ぎ資料として既存7枚と併存する。**
    7枚をいつ役目終了にするかは決めていない。案2のポインタで「旧」と示すのみ。
13. **helen 以外3案件には `state.json` が無い。** 承認D で対象群ごとに判断する。
    水着化の作業ルートは KB 内にあり稼働中（実測 2026-08-30 16:30 / 16:31）。
14. **成果物 Inbox への申告が brainstorm 封鎖の誤検知で通らない**（2026-08-30 実測）。
15. **サブエージェントの reasoning effort を指定する手段がメインエージェント側に無い。**

---

## 12. 捨てた案と理由

| 捨てた案 | 理由 |
| --- | --- |
| 客観的な豆知識の entity 化 | 「効果ないならいらない」（武田さん 2026-08-30） |
| `run-state.json` を正本にする | 武田さんの判断を JSON に書くことになり人が読めなくなる |
| **`run-state.common.json` を新設する（rev.2〜3 の4節）** | **相手の承認済み計画と正本が衝突する**（2026-08-31 武田さん判断で相手側へ寄せた） |
| 対応表案（生成側に案件ごとの読み取り規則） | 案件が増えるたび追記が要り、忘れると黙って落ちる |
| 自動生成を削り、中身を読まずに取れるものだけにする | 最大の効果が半分になる |
| 既存ページに一覧を複製する | 更新場所が2つになり必ず片方が古くなる |
| 壊れ#4 を冒頭1行だけで直す | 本文が「自分が正本」と言い続けるため問題が残る（武田さん判断で案1） |
| 壊れ#4 を別フォルダ退避で直す | 実パス参照が全部切れる。新ディレクトリは構造変更 |
| **2つの計画を統合する / 本計画を取り下げる** | 統合は相手の実行承認を崩す。取り下げると案2・壊れ#4・他3案件が未解決で残る（2026-08-31 武田さん判断） |

---

## 13. 承認関所

| 関所 | 何を承認するか | 種類 |
| --- | --- | --- |
| A-0 | rev.4 の縮小方針（相手との分担・`common` 廃止） | 方針 |
| A-1 | 雛形の形、frontmatter 欄の新設、`logs/` の新設（KB の構造決定） | 方針 |
| A-2 | `state.json` 拡張要求（4節）を相手へ渡すこと | 方針 |
| B-2 | 1.3 の仕分け結果と置換案（変更前後の全文） | 実行 |
| C | 正解表の承認と、閉じた試験の合否判定 | 実行 |
| D | 残り3案件への展開 | 実行 |

---

## 14. 関連ファイル（実パス）

- ブレストメモ（正本）:
  `…/LLM Knowledge Base _01/wiki/analyses/brainstorm/project-hub-index/_index.md`
- **相手の承認済み計画**:
  `…/LLM Knowledge Base _01/wiki/builds/gf2-helen-repro-execution-audit-plan-20260830.md`
- **相手の未承認計画（一本化）**:
  `…/LLM Knowledge Base _01/wiki/builds/gf2-helen-deliverable-unified-route-plan-20260831.md`
- 1回目のレビュー: `…/wiki/analyses/project-current-state-page-plan-review-20260830.md`
- 2回目のレビュー: `…/wiki/analyses/project-current-state-page-plan-review2-20260830.md`
- 設計の説明: `…/wiki/_attachments/project-hub-index/20260830-handoff-mechanism-design.html`
- 壊れ#4 の説明: `…/wiki/_attachments/project-hub-index/20260830-four-files-claim-master.html`
- helen の全容: `…/wiki/_attachments/project-hub-index/20260830-helen-repro-v51-overview.html`
- helen の生データ:
  `/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/01_イラスト/07_3D資料/gf2-helen-starlit-waltz/06_repro-v51/run-state.json`
- 既存の判定スクリプト（応用元）: `/Users/takedayousuke/.claude/skills/brainstorm/brainstorm_guard.py`

> 上の `…` は KB ルート
> `/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01` を指す。
