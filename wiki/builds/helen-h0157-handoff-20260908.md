# Helen H0157 引き継ぎ資料（2026-09-08）

別実行者がこの資料だけで動けるよう、目的・現状・いきさつ・証拠・未決まりを一か所に集める。
各文の末尾に確からしさを付ける。`実測`は機械で測った値、`記録`は残っている文書、
`推論`は未検証の考えである。推論は推論と書く。

## 1. 目的と武田さんの意図

- 寮の見た目（Helen）を Blender 上で再現すること。
- 成果物は コード探索→回収→blend への適用が済んだ blender ファイルであること。
- 開いて確認できる状態で渡すこと。手順の学習を要求しないこと。
- 調査は Blender 上で問題が解消するまで終えないこと。
- 品質の主張は書かないこと。判断は武田さんが行う。

## 2. 現状の到達点

- 指：候補 f184 以降で 30 本全て原作連鎖と 0.00 度で一致（4 枠で実測）。
  49 枠の接写比べで扇形と鉤形の差が出る（実測）。
- 光の等級：対比と彩度だけ候補へ結線済み（検証済み・門合格・実測）。
  顔の白飛びは描画で 0.39334 が 0.0 になった（実測）。
- 表の選択図：共用表など 13 枚を取出済み（実測）。結線は黒で停止中（実測）。
- 原本は無変更で版が一致する（実測・毎回確認）。

## 3. 汚染の時系列

- 08-24：f129 が指を含む 330 骨の world 一致を 0.1mm 以下で検証（記録）。
  対象は H 適用前の版である（記録・日付の照合）。
- 08-25：H 変換が評価済み値から再変換し本体版が変わった（記録・本人の記載）。
  指 2・3 節と拇指が原作連鎖から 21〜180 度ずれた（実測・現行本体）。
- 09-05〜06：T4・T5 の確定記録、仮置き候補、等級候補（記録）。
- 09-07：段取り書、弱形記録、自律深掘り、封 3 件（記録）。
- 09-07〜08：186 束走査で 355 件の当たり、素体既定と場面上書きの差分図、
  表の選択図、8 束の場面表、場面設定の実値（実測・いずれも台帳あり）。
- 09-08：指 20 本の単発 rebuild（30 本全て 0.00 度・実測）、表結線の実装と
  黒停止の切り分け（実測）、開封段取り（目印・枠・鏡・文・実測）。

## 4. 汚染候補

### M1 評価済み値の再変換（実測・記録）

- f151 の記録本文に「fcurve 評価後の値から」とある（記録）。
- c02 の角度変換は注釈と実装の順序が食い違う（実測・コードの目視）。
- 現行本体の指 2 節は原作の縦曲げに対し横曲げになっている
  （49 枠の実測・軸が 90 度違う）。
- f184 の rebuild は単発であり 30 本全て 0.00 度に戻る（実測）。

### M2 表標本の黒（実測）

- 合成器の画像入力と MapUV は headless で黒になる（4 連続実測）。
- 描画層は正常である（実測・平均 64.1 の対照つき）。
- 陰影器の画像入力も束内包表は黒、盤上表は見える（実測）。
- 論理自体は参照実装と十億分の一で一致する（実測）。

### M3 仮定値の幾何の破綻（実測）

- f175 の仮定値は実データで負の補間率を出す（実測・青が負になる）。
- 表自体は 2 次元の検査図である（実測・相関と目視）。
- 仮定のまま結線しないこと（結論・門の方針どおり）。

### M4 受渡しの衛生不良（実測・いずれも発見して修理済み）

- 画素同一の版を渡した（f183・f185・記録）。
- 開く枠が 300 だった（f185 で 206、f187 で 49 に直した・記録）。
- 描画が別鏡で出た（f187 の主描画・再描画して一致を実証・記録）。
- 出力形式が EXR のまま残った（PNG に戻した・記録）。
- 結線数の数え違い（重複 3 件・前版と同一・記録）。
- 相対経路の描画が紛失した（絶対経路に直した・記録）。

### M5 検証の循環の疑い（推論）

- f129 の検証と c02 の実装が同じ符号系を使うため、両者の一致が
  正しさの証明になっていない可能性がある（推論）。
- 指 1 節と腕は 0.00 度で一致する（実測）。問題は指先の軸である（実測）。

### M6 枠と原画の対応の疑い（推論）

- 206 枠と原画 32 の対応は作業仮定であり、厳密な同時刻証明はない（推論）。
- 原画は 1 秒刻みしかない（記録）。49 枠の直接の原画はない（記録）。

## 5. ファイル台帳

原本は無変更（`04ef8b79b3fa5b64`・実測）。候補は次である。

- f181 等級：`0d5827a9d0801417`（対比と彩度の結線・記録あり）。
- f183 分離待機：`9e9c9441b61f1f0e`（表 2 枚内包・未結線・記録あり）。
- f184 指 rebuild：`4fa9f079d3d3d895`（30 本 0.00 度・記録あり）。
- f185 開枠版：`be204de683221a1a`（206 で開く・画素同一・記録あり）。
- f186 表結線：`057ab928eda9c522`（分岐と文件出力・合成器不変・記録あり）。
- f187 等級場面：`15a3e2083e99689f`（最新・49 で開く・目印つき・記録あり）。

場所は次の通りである。

```python
path = "/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/01_イラスト/07_3D資料/gf2-helen-starlit-waltz/06_repro-v51/blends/_candidate-postchain/helen-h0157-repro__04ef8b79b3fa5b64_f187-lutgrade.blend"
```

```python
path = "/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/01_イラスト/07_3D資料/gf2-helen-starlit-waltz/06_repro-v51/ledger/t16-slot-map.json"
```

```python
path = "/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/01_イラスト/07_3D資料/gf2-helen-starlit-waltz/06_repro-v51/logs/f186-lutwire.json"
```

```python
path = "/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/01_イラスト/07_3D資料/gf2-helen-starlit-waltz/06_repro-v51/logs/f184-fingerchain-gates.json"
```

## 6. 門と制約

- 転写門は未検証の実装を止める（記録）。検証済み 2 値は主張して合格中（実測）。
- 封 3 件は再調査の停止である（記録）。本体承認の規則は opencode 範囲で
  停止中である（記録）。実機操作と外部取得の同意は停止していない（記録）。
- 触れてはいけないもの：`index.md`・`log.md`・`raw/` 以下・`AGENTS.md`・
  `CLAUDE.md`・`quality-gate.json`・`tools/` 以下・共有の design-system（起動路の指定）。
- 未決まり：blender 編集の許可が候補だけか原本を含むか（武田さんの発言は
  両読みできる・記録）。原本への適用は明示があるまで行わないこと。

## 7. 次の調査への入口

- M1 の残り：拇指の鏡像経路の再検証（f184 で 0.00 度だが経路の正当性は別）。
- M2 の残り：武田さんの Blender での F12 確認（未測定）、陰影器経路の完成。
- M3 の残り：表変数の真値（場面束か実行時確保）。
- 光の等級：filmic 3 値（検証済み・未結線）が最大の可視候補。
- 見る手順：最新版を開く（49 枠）→ 目印で飛ぶ → F12 を押す。

## 8. 用語の定義

- 原本：壊したときに戻る場所。凍結して版で保つ。
- 候補：試作だけを書く場所。目視の対象。
- 門：欠けや根拠不足を止める検査と記録。
- 弱形：範囲と件数と陽性対照つきの不検出の書き方。
- 汚染：この資料では「成果物に結果が載らなくなった状態」を指す。
  原因の断定ではなく、M1〜M6 の検証対象を指す。
