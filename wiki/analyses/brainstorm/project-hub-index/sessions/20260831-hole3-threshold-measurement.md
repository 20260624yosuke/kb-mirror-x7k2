---
type: analysis
status: active
confidence: medium
evidence_level: source-backed
last_reviewed: 2026-08-31
---

# 2026-08-31 穴3の閾値の根拠を数えた（セッション3）

親メモ: `/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01/wiki/analyses/brainstorm/project-hub-index/_index.md`

## 依頼（武田さん・このセッション冒頭）

> `/brainstorm` `/html`、親メモと前回の引き継ぎ（`20260831-144000--今すぐやること.md`）を見て、タスクを進めてください。

## やったこと

前回の残作業「穴3の閾値が未定。過去の成果物で『落ちるはずの回数』を数える工程が先」を実行した。
5検査計画（`wiki/builds/brainstorm-five-guards-plan-20260831.md`）の完成条件3に相当する計測。

- 対象: `~/.claude/projects/` の3プロジェクトフォルダの全会話記録（jsonl・計108本超）。
- 数えたもの: HTML ファイルへの `Write` / `Edit` 操作の全履歴。`Edit` は
  `old_string` が `new_string` に丸ごと含まれるか（＝純粋な追記）で仕分け、
  ファイルごとに「純粋追記の最大連続回数」を出した。
- 計測スクリプトは `/tmp` に置いて実行（KB・スキル本体は一切触っていない）。

## 結果（すべて実測）

- 母集団: HTML **30ファイル**、操作 **48回**（Write 33 / Edit 15、うち純粋追記 **6**）。
- 純粋追記の最大連続回数: 全履歴で **2** が最大。出たのは `20260831-remaining-four-tasks.html`
  **だけ**（系列 `WWAAEEAAW`、2連続が2回）。他の29ファイルは最大1以下。
- 良い版3枚（`20260831-brainstorm-three-holes-fixed` / `20260830-handoff-mechanism-design` /
  `20260831-where-i-drifted`）は純粋追記 **0回**。

| 閾値 | 過去48操作で止まった回数 | うち実際の失敗ファイル | うち誤検知 |
| --- | --- | --- | --- |
| T=1（追記1回で止める） | 6回（3ファイル） | 4回 | 2回（`20260830-three-approvals-explained` ×1、`tools/` の `popup.html` ×1） |
| **T=2（2連続で止める）** | **2回（1ファイル）** | **2回** | **0** |
| T=3（3連続で止める） | 0回 | 0回 | —（実際の失敗すら捕捉できない） |

## 限界（正直に）

- 母集団は **Claude Code の記録のみ**。Kimi Code（この会話）や Codex のセッションは含まない。
- 30ファイル・48操作は**少ない**。T=2 の誤検知ゼロは「この範囲では」の話。
- `Write` の全面書き換えは検査の設計上そもそも対象外。
- `popup.html` は `tools/` のブラウザ拡張のファイルで説明 HTML ではない。
  「成果物の置き場をどのフォルダにするか」は計画でも未確定のまま。

## 次の判断（武田さんに聞く）

1. 5検査の**実行の承認**（計画の「終わったら次に取る承認」1番）。
2. 穴3の閾値: この数字を踏まえ **T=2 を推奨**として承認を取る（同2番）。

## カード記録（2026-08-31 セッション3）

- 上の2問をカードで聞いた。回答には「承認しない・先にサブエージェントへ計画をレビューさせる
  （バイアスのかかる指示は禁止・仕様と実装の不一致を重点的に）」という詳細な入力があったが、
  **確認の質問で「いいえ、押し間違えた」が返ったため、ルールに従い回答は全件破棄**。
  サブエージェントの起動も実施していない。同じ内容を聞き直した。

### 聞き直しの回答（確認「はい」で確定）

> 計画のレビューはさせてください。穴3の倒置がよくわかりません。問題なら解決して。
> 問題を放置することを禁止します。なぜこうなったかの調査と解決。
> 解決は機械的な監査以外の方法を禁止します。
> 解決法の仕組みが、有効に機能する事実と根拠が揃ってない場合は禁止します。

> （閾値の問いに）前のカードで答えました

- 確定事項: **実行の承認は保留。先にサブエージェントが計画をレビューする。**
  レビュー指示はメインエージェント（この会話の私）が送る。バイアスをかけない。
  重点は「仕様と実装の不一致」。
- **「穴3の閾値がよくわからない」とのことなので、専門用語なしの説明をやり直してから聞き直す。**
  （「倒置」は「閾値」のことと読んだ。読み違いなら訂正されたい）

### 中断と再開（2026-08-31）

- レビュー用サブエージェント（plan 型）を起動したが、**起動直後に利用上限（403）で停止**。
  レビュー結果は得られていない。武田さんの指示で同じ地点から再開し、再度起動した。

### 独立レビュー結果（2026-08-31・サブエージェント、無バイアス指示）

所見: **現状のまま実装に渡せない**（critical 2 / major 4 / minor 5）。
フック登録の記述と実装の一致・完成条件1〜3の検証可能性は問題なしと確認された。

- **C-1 穴3の適用範囲が未定義。** 閾値の根拠は HTML 限定の計測なのに、計画本文の検査定義は
  全 Edit に読める。除外条項は穴1・穴2だけを名指し。**字面どおり全 Edit に掛けると、
  brainstorm の中核動作（毎ターンのメモ追記＝典型的に純粋追記）が2連続で止まり、スキル自身を壊す。**
- **C-2 「成果物の置き場」が未確定のまま完成条件に決定が含まれていない。** 実際の HTML 成果物は
  `wiki/_attachments/` にあるが、現行コードは wiki 配下を成果物に**数えない**（真逆の定義）。
  置き場を決めないと穴2・穴4・穴5の判定式が書けず、発火しえない設計のまま完成条件2を
  原理的に満たせない。
- **M-1** 穴1の「指示を選んだカード回答」の機械判定が未定義。緩く実装すれば恒真で素通り、
  厳しくすれば計画の禁止事項「軸を機械に判断させない」と衝突。
- **M-2** 穴5の前提「file:// では CSS が落ちる」が技術的に疑わしい（相対パスの CSS は普通読める）。
  原因特定が無く、「画面を取った記録」の検出根拠も未定義。儀式化して本当の欠陥を見ない危険。
- **M-3** 計画書の「いまの検査はスキル起動しか見ていない」は**事実と異なる**
  （現行 guard-stop はメモ未反映・カード前本文・実装ゼロ等を検査済み）。完成条件5の対象集合が曖昧。
- **M-4** 新検査の登録先（SKILL.md 側フックか settings.json 常駐か）とゲート条件が未定義。
  常駐させると brainstorm 以外の通常作業にも発火しうる。
- minor 5件（層数の表記・実測値の引用不一致「8回 vs 9操作」・再現不能な実測値・
  既存の未配線コード `_guard_done_promotion`・穴2のリンク形式が KB 規約とずれうる）。

→ **次の工程: 計画を改訂（適用範囲・置き場・判定式・登録先・棚卸しを追記）→ 再レビュー →
実行の承認を取り直す。** 実装には着手しない。

### カード承認（2026-08-31・確認「はい」で確定）

1. **「改訂して進める」を承認。** 計画に critical/major への対応を追記 → 再レビュー →
   実行の承認を取り直す流れ。
2. **穴3の閾値は T=2 で承認。** 対象は説明 HTML の編集に限定する前提（計画の改訂に書く）。
