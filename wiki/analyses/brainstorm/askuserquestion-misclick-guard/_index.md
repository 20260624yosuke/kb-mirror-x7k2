---
type: analysis
status: active
confidence: medium
evidence_level: user-stated
last_reviewed: 2026-08-30
brainstorm_status: active
scope:
  - /Users/takedayousuke/.claude
  - /Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01
entry_paths:
  - /Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01/wiki/analyses/brainstorm/askuserquestion-misclick-guard/_index.md
  - /Users/takedayousuke/.claude/settings.json
background_paths:
  - /Users/takedayousuke/.claude/skills/brainstorm/SKILL.md
  - /Users/takedayousuke/.claude/skills/brainstorm/brainstorm_guard.py
---

# 承認カードの誤クリック対策

## 武田さんの考え

> アスクユーザークエスチョンの承認カードが出た時、その選択の送信コマンドを Enter じゃないと
> 機能しないようにしたいんだけど可能？ 誤クリックで推論の方向がずれるのが不快

- 困りごとは「クリックが即送信されること」そのものではなく、**誤クリックで推論の方向がずれる**こと。

## 決まったこと

### 2026-08-30 武田さんの指示（A/B/C は承認せず）

> ならわかった。選択カードがこれでよろしいですか？っていう確認を必ずもう一枚増やすようにしてほしい。
> 承認待ちって複数で出せるじゃん。
> 対象はこの brainstorm のスキルだけで行います。それ以外での動作は現状禁止します。

- **確認カードを必ずもう1枚出す。** 選択したら、そのまま進まず「これでよろしいですか」を挟む。
- **適用範囲は brainstorm スキルの中だけ。** 他の作業・他のスキルでの承認カードには入れない
  （現状禁止）。

### 2026-08-30 追加の指示

> まずは x の方針でいい。でも使用感が悪かったら y を導入したいから、それは記録しておいて。
> そっちの方針で再開できるように。

- **確認の出し方は X（同じカードの中に確認の質問を並べる）で進める。**
- **Y（次の応答で確認カードを1枚出す）は保留。使用感が悪ければ Y へ切り替える。**
  切り替え時に必要な情報は下の「Y へ切り替えるときの入口」に書いた。

> なお承認カードの確認はルールではなくて、ここに関してハーネスをちゃんと組まないと、
> 目標の成果物にたどり着けないという問題です。（中略）この内容の中の、
> **推論ではなく機械的な仕組みとして動作する**ようにしてください。

- 「毎回確認質問を付ける」を**私の心がけ（文言ルール）にしない**。機械が確認する。

## Y へ切り替えるときの入口（保留案・2026-08-30）

- Y = 選択が届いた次の応答で「◯◯を選ばれました。これで進めます。よろしいですか」の
  カードを**別途1枚**出す方式。画面が切り替わるぶん誤クリックに気づけるが、毎回1往復増える。
- 切り替えの判断材料: X を使ってみて「同じ画面の2問目もそのまま押してしまう」が実際に起きたか。
- 実装の差: X はカードを作る側の検査（確認質問が入っているか）だけで足りる。
  Y はそれに加えて「選択が届いた直後の応答で作業へ進むことを止める」検査が要る。

## まだ決まってないこと

- 機械化候補（下の「機械化できるもの」）のうち、今回どこまでを実装対象にするか。
- `AskUserQuestion` で PreToolUse フックが実際に発火するか（**未確認**。確認は実装側の第1歩）。
  ※当初 PostToolUse と書いていたが、拒否は PreToolUse でしか実現できないため訂正（2026-08-30）。

## 事実確認（2026-08-30・ファイルを直接読んだ）

1. **「クリック送信を無効化して Enter だけにする」設定は存在しない。**
   `~/.claude/keybindings.json` は未作成。そもそも keybindings.json が扱うのは
   **キーの割り当て**であって、カードのマウスクリックは対象外。クリックを殺す設定項目は無い。
2. 現行の `~/.claude/settings.json` に `AskUserQuestion` を対象にしたフックは**無い**
   （`AskUserQuestion` の文字列は brainstorm スキル側のファイルにしか出てこない）。
3. よって「送信手段を Enter に限定する」は**不可**。実現できるのは
   **誤クリックしても方向が確定しない（復唱して確認するまで私が動かない）**という形の担保。

## 候補（それぞれ失うもの付き）

- **A. PostToolUse フックで「復唱→確認まで進むな」を機械化**
  `AskUserQuestion` の直後に、私の文脈へ「選んだ内容を1行で復唱し、承認されるまで作業を進めるな」を
  自動で差し込む。誤クリックしても、次の私の発言は復唱だけなので、その場で言い直せる。
  失うもの: 誤クリックしていない時も **毎回1往復増える**。カードの手数が2倍になる。
- **B. 承認カードを使わず、番号付きの文章で選ばせる**
  クリック対象が消えるので誤クリックが原理的に起きない。
  失うもの: brainstorm の `guard-stop`（カード無しで会話を閉じるのを機械的に止める仕組み）が
  効かなくなる。**会話を勝手に閉じる事故が戻る。**
- **C. 選択肢の1番目を必ず「まだ決めない／説明して」にする**
  誤クリックの当たりどころを無害な肢にする。フックもスクリプトも不要。
  失うもの: 推奨案を1番目に置く現行の書き方が崩れ、毎回1クリック余分。
  そして**誤クリック自体は防げない**（2番目以降を誤爆したら同じこと）。

## 確認カードの2通り（2026-08-30・要決定）

`AskUserQuestion` は1回の呼び出しで最大4問を並べられる。ここから2通りの実装がある。

- **X. 同じカードの中に確認の質問を並べる（1回のやり取りで完結）**
  1問目「どれにするか」、2問目「上の選択でよろしいですか」を同時に表示する。
  やり取りは増えない。ただし2問とも同じ画面にあるので、**手が滑った流れでそのまま2問目も
  押してしまうと素通りする**。効き目は弱い。
- **Y. 選択が届いた次の応答で、確認カードを1枚出す（カード2枚）**
  「◯◯を選ばれました。これで進めます。よろしいですか」を別のカードとして出す。
  画面が一度切り替わり、選んだ内容が文章で目に入るので、**誤クリックに気づける**。
  そのぶん毎回1往復増える。

## 捨てた案と理由

- **A/B/C（復唱フック・番号選択・1番目を無害肢）→ いずれも不採用**（2026-08-30 武田さん）。
  代わりに「確認カードをもう1枚」で担保する。

- キーバインド設定でクリック送信を殺す案 → **不可能**。上の事実確認1のとおり、
  クリックはキー割り当ての対象外で、無効化する設定項目が存在しない。

## 直した記録

- 2026-08-30: 「まだ決まってないこと」の PostToolUse → PreToolUse を訂正（旧案 A 由来の残り）。

## 再開の入口（実パス）

- このメモ: /Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/LLM Knowledge Base _01/wiki/analyses/brainstorm/askuserquestion-misclick-guard/_index.md
- フック登録先: /Users/takedayousuke/.claude/settings.json
- brainstorm 規則本体: /Users/takedayousuke/.claude/skills/brainstorm/SKILL.md

## 実装への申し送り

**2026-08-30 承認: 実装対象は G1 + G4 の2本。対象は Claude 側の brainstorm のみ。**
（G2・G3 は今回入れない。テーマの範囲も広げない＝このフォルダのまま。）

### 完成条件

1. **G1: 確認質問の無い承認カードを機械的に拒否する。**
   - `brainstorm` 作動中に `AskUserQuestion` を呼んだとき、**確認の質問が1問も入っていなければ
     ツール呼び出しを拒否**し、拒否メッセージで「確認質問を足して出し直せ」と返す。
   - 「確認の質問」の判定は決定論的に行う（例: 質問文に確認を表す固定の印が含まれること）。
     印の文字列は実装側が決めてよいが、**LLM の意味判断に頼らないこと**。
   - brainstorm が作動していないときは**何もしない**（他の作業・他のスキルでは現状禁止。
     2026-08-30 武田さん指示）。
   - 実機で確認すること: ①確認質問なしのカードが実際に止まる ②確認質問ありのカードは通る
     ③brainstorm 外のカードは素通りする。

2. **G4: 指摘を検査へ変換する工程を仕組みにする。**
   - 親メモに `## 機械化した指摘` 節を設ける。1件につき「指摘の内容 / 再発しうるか /
     機械判定できるか / 変換先（検査名 or 人間判断として残す）」を書く。
   - `brainstorm_status` を `ready` へ上げるとき、**この節が存在すること**を機械で確認する
     （該当が無い回は `なし` と書けば通る。節そのものの欠落だけを止める）。
   - 目的は「同じ指摘を武田さんが二度しないこと」。節を埋めるのは私の作業で、
     武田さんに棚卸しをさせない。

### 絶対にやってはいけないこと

- **文言ルールだけで担保して「できた」と報告しないこと。** 今回の依頼の核心は
  「推論ではなく機械的な仕組みとして動作させる」こと（2026-08-30 武田さん）。
- **他の LLM のファイルを触らないこと。** Codex 側（`~/.codex/skills/brainstorm/`）は別実体で、
  同期しない。条件を渡して向こうで組ませる（時期は未定）。
- **brainstorm 以外の場面で承認カードに介入しないこと。** 現状禁止。
- 既存の封鎖（guard-write）・停止（guard-stop）・到達性監査を弱めて解決したことにしないこと。
- G2・G3・工程分割・監査役 LLM を、頼まれていないのに一緒に入れないこと。

### 捨てた案と理由（蒸し返さないため）

- Enter でしか送信できなくする → **不可能**。クリックはキー割り当ての対象外で、
  無効化する設定項目が存在しない（2026-08-30 実測）。
- 復唱フック／番号選択／1番目を無害肢にする（旧 A/B/C）→ 武田さんが不採用。
- Y（次の応答で確認カードを別途1枚）→ **保留**。X の使用感が悪ければ切り替える。
  切り替え手順は上の「Y へ切り替えるときの入口」。
- 監査役 LLM を挟む → 読む対象が一段増えるだけ。8-29 の「サブエージェントへ投げない」と同じ結論。

### 終わったら次に取る承認

1. G1・G4 の実機確認の結果を見て、**X の使用感が許容できるか**（悪ければ Y へ切り替え）。
2. **Codex へ同じ条件を渡す時期**（今は未定。渡すときは系統Aの移植作業と合わせる）。

## 機械化した指摘

| 指摘（2026-08-30） | 再発しうるか | 機械判定できるか | 変換先 |
|---|---|---|---|
| 承認カードに確認をもう1枚必ず付けてほしい | する（私が忘れる） | できる | **G1**（確認質問の無いカードを拒否） |
| 推論ではなく機械的な仕組みにしてほしい | する | 個別には不可 | **G4**（指摘を毎回この表へ落とす工程） |
| brainstorm 以外では動作させるな | する | できる | G1 の作動条件（brainstorm 中のみ） |

## セッションメモ（子）

（なし）

## 関連リンク

- brainstorm スキル正本: wiki/builds/brainstorm-skill.md


---

# レビュー・検証ボトルネック（2026-08-30 追加テーマ）

出典: /Users/takedayousuke/llm-uploads/20260828-223742--AI開発における-レビュー-検証ボトルネック-を現在のプロジェクト計画へ適用す.md
（その一次資料: /Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/05_claude/claude_llm_wiki/knowledge用_inbox/Clippings/AIの出力をレビューするのがしんどい問題.md）

資料の中心命題: **改善対象は「AI にもっと仕事をさせること」ではなく、
「AI の仕事を人間が確認しなくても済む条件を増やすこと」。**
判断基準は「これを入れると武田さんが確認しなければならないものが具体的に何個減るか」。

## 現状評価（brainstorm ハーネスへの対応表・2026-08-30 実測）

| # | 資料の要素 | 現状 | 実測の根拠 |
|---|---|---|---|
| 1 | 大きな計画と小さな実行の分離 | 部分的 | brainstorm は計画まで、実装は別会話。ただし工程分割は機械強制なし |
| 2 | Phase 単位の作業 | 存在しない | メモに Phase の概念がない |
| 3 | Phase ごとの完了条件 | **部分的（穴）** | `## 実装への申し送り` に書く規則はあるが、**書いたかを機械が見ていない** |
| 4 | セッション分離 | 存在 | 実装は新しい会話で行う規則＋封鎖 |
| 5 | 状態ベースの引き継ぎ | 存在 | 親メモ＋SessionStart/UserPromptSubmit の再注入 |
| 6 | 決定論的な検証 | 部分的 | guard-write 封鎖・guard-stop・到達性監査はある。成果物の中身は見ていない |
| 7 | LLM 監査の役割限定 | 該当なし | サブエージェント監査は使わない方針（8-29 決定） |
| 8 | 人間判断事項の抽出 | 存在しない | カードで何を聞くかは私の裁量まかせ |
| 9 | 人間の指摘→検証ルールへの変換 | **存在しない（最大の穴）** | 今は CLAUDE.md やメモリへ文言を足すだけ。文言は忘れる |
| 10 | hook による検証強制 | 存在 | settings.json に登録済み |
| 11 | 報告の認知負荷削減 | 部分的 | 「報告は3分割」は文言ルールのみ。機械化されていない |
| 12 | Phase 完了ごとの継続改善 | 存在しない | 棚卸しの工程がない |

## 最大のギャップ

**#9。** 武田さんが一度指摘したことが、次回また武田さんの目で確認しないと守られない。
今日の「誤クリックで方向がずれる」も、放っておけば #9 の一例になる
（「毎回確認を付けます」と書いて、私が忘れる）。

## 機械化できるもの（人間の確認 → 決定論的な検査）

いずれも `brainstorm_guard.py` に足せる。カッコ内は「これで武田さんが確認しなくてよくなるもの」。

- **G1. 確認質問の無い承認カードを拒否する**（PreToolUse / AskUserQuestion）。
  今日決めた X 方針の正体。私が忘れても、確認質問が無いカードは出せない。
  （→ 承認カードを毎回見比べる必要が消える）
- **G2. `## 実装への申し送り` の中身検査**。`ready` へ上げるとき、完成条件・禁止事項・
  捨てた案が**空でないこと**を機械で見る。今は到達性（パスが開けるか）しか見ていない。
  （→ 申し送りが空のまま実装へ渡る事故の確認が消える）
- **G3. 選択肢に「失うもの」が書かれているかの検査**。カードの各 description に
  代償の記述があるかを見る。CLAUDE.md #3 の規則の機械化。
  （→ 代償の見えない二択を武田さんが指摘する手間が消える）
- **G4. 指摘の棚卸しを工程にする**。武田さんの指摘が出たら、メモの
  `## 機械化した指摘` へ「指摘・再発しうるか・機械判定できるか」を書き、
  できるものは G1〜G3 と同じ形でスクリプトへ足す。
  （→ 同じ指摘を二度しなくてよくなる。#9 の本体）

## 導入しない方がよいもの（資料にあるがこの案件では不要）

- **Reviewer Agent（監査役 LLM）**。読む対象が一段増えるだけ。8-29 に「サブエージェントへ
  投げない」と決めた判断と同じ結論。
- **Phase 分割の機械化（#1/#2/#12）を今回いっしょに入れること**。brainstorm は計画までの
  スキルで、実装工程を持たない。入れると仕組みの維持自体が新しい負担になる。

## 他 LLM への同期（2026-08-30 実測・武田さんの質問への回答）

**同期されない。今回の修正は Claude 側だけで完結し、Codex には自動で反映されない。**

- Claude: `/Users/takedayousuke/.claude/skills/brainstorm/`（SKILL.md + brainstorm_guard.py）
- Codex: `/Users/takedayousuke/.codex/skills/brainstorm/`（SKILL.md + `scripts/` + `agents/`。
  2026-08-29 17:05 に移植済み。**Claude 側とは別実体で、共有ファイルは無い**）
- Kimi: **未移植**。`~/.kimi-code/config.toml` には休止済みの hold のフックだけが残っている
- opencode: **未移植**。`~/.config/opencode/opencode.jsonc` にフック登録が無い

これは事故ではなく、2026-08-29 に武田さんが承認した「スキル本体は LLM ごとに独立」という方針の
とおりの状態（正本: wiki/analyses/brainstorm/brainstorm-skill-portability/brainstorm-brainstorm-skill-portability.md）。
よって今回決めた条件は、Codex へは**同じ条件を渡して向こうで組ませる**必要がある。
