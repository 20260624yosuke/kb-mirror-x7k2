---
type: analysis
status: active
confidence: high
evidence_level: source-backed
last_reviewed: 2026-08-24
sources:
  - gf2-char-extract-handoff
---

# gf2 キャラ×服装一覧と抽出選定相談（2026-08-24 セッション記録）

> [!warning] 並行セッションへの配慮
> 本ページは **opencode の選定相談セッション**の記録。同じく 2026-08-24 に **別セッションが
> `gf2-char-extract` の Step 1（抽出ドライバ）を稼働中**であり、正本 handoff
> （[[gf2-char-extract-handoff]]）・`run-state.json`・プロジェクトフォルダは**本セッションでは
> 一切変更していない**。Step 1 の進捗は handoff ページ側（作業セッション）で更新されるのが正。
> 本ページとの突合は両セッション完了後に [[gf2-char-extract-handoff]] を通じて行う。

## 要約

ヘレン光再現とは独立に「原作3Dからキャラ服装を作画資料として抽出する」話のうち、**どのキャラの
どの服を抽出するか**の選定相談。web 情報のみでの分類はハルシネーションリスクが指摘されたため、
実データベースの一覧をサブエージェントで作成した。

## 経緯（このセッションで起きたこと）

1. `/hold` 起動。「3D を資料にしたい理由の一つが服装資料化。ox(opencode) 利用期限もあるので
   何のキャラを書き起こすか相談したい」
2. 選定基準の回答: **描きたい服・推し優先**。目標体数は複数体・理想は全キャラ
   （ビルドクオリティが高い旨の武田さん評価）
3. dorm 質問に対する訂正: 「スキンかスキンじゃないか」の軸であり、dorm かどうかではない。
   web リサーチ(IOP Wiki/Gamerch)で Combat Suits／Outfits／Swimsuit の分類を確認したが
   バージョン差検証は不十分だった
4. 武田さん指摘「**ハルシネーションは調査不足が原因**。どうケアできる？」→ 対策として
   ①実データ読解 ②出典格付け ③確度ラベル ④実機共同確認の4層を提示
5. 「キャラ×服装一覧作成」は**裏(サブエージェント)**へ回す判断が承認され、起動 → 完了
6. hold スキルの扱いについて質問があり、「明示起動された時点で作動中として扱う」方針を承認

## サブエージェント調査の結果（source-backed / 詳細は添付）

- GunData.bytes から戦術人形 59 体のデフォルトモデルコード、衣装バリアント 174 コードを確認
- 一覧表: **56キャラ × 125バリアント行（代替スキン 69 コード）**
- 命名規則 `<キャラ名><SSR/SR>01<スキン番号2桁>`（例 HelenSSR0101=スキン1）。Helen で陽性対照 PASS
- 代替スキン多め: クルカイ4／グローザ3／マキアート3／ネメシス3。2着組14キャラ
- `ClothesDutyData.bytes` 内に SkinType 分類文字列（`Swimsuit`/`Romantic`/
  `Special_MacqiatoSSR02` 等）を確認。**ただしコード↔ジャンル対応は未解読**
- 正本テーブル: `wiki/_attachments/gf2-costume-inventory/table.json`
  （レポート: 同フォルダ `report.md`。元ファイルは temp 領域のため wiki 側へ複製して恒久化）

## 矛盾・未確定

- **スキン番号↔ゲーム内表示名の対応は全部未確認**
- 衣装ジャンル（水着/私服/ウェディング等）の個別判定は全部未確認
- 名前ヒット≠完全在庫（HelenSSR0101 材質0件の前例）。バリアント単位の抽出可否は未検証
- Lene/Mityl/Soppo/Welrod/OTs14/BattleVoymastina/NemesisGnosis の和名は公式サイト一覧になし（未確認）
- RX_/CF_ 接頭辞の意味は未確認。MacqiatoSSR02 は設定文字列のみで bundle 未確認
- 次の一手: `Table/*.bytes`（SkinTypeData/ClothesDutyData/GunData）のバイナリ解析で
  「スキン番号↔衣装名↔ジャンル」の正式対応表を作る（report.md §4 参照）

## 後続展開（2026-08-24 午後・セッション再開分）

セッションが死亡→同日中に新セッションで再開し、以下を実施。**最終的にビューア成果物は
武田さん不採用判断**（「品質良くないから自分で探す」）。データ表は選定材料として残置。

### 経緯（再開分）

7. 優先度の軸を相談 → 武田さんが序列を直感で提示: **「服装 > ポーズ > 体型 > パーツ > 顔」**。
   言語化を複数回試みたが（固有性×代替困難さ／工程順／代用困難度／物理複雑さ）いずれも
   武田さんに響かず。武田さん自身の語: **「立体感の把握」が本質**で、顔が下位なのは
   「比較的苦にしない（＝すでに描ける）」から。**資料の基準を自分の中で持っていない**
   ことが優先度曖昧さの正体と判明したが、基準の確定には至らず
8. オタクコンテンツの服装カテゴリ一覧を作成（既出7種以外: 和装・職業・ファンタジー・
   運動・日常私服・ストリート系・チャイナ・サイバー等）
9. **Table/*.bytes 解読**（裏作業）: ClothesData 146/146件・SkinTypeData 6/6件・
   ClothesDutyData 21特典クラス・LangPackage JP約35.6万エントリを復元。
   ただし**スキン個別のジャンル割当フィールドはゲームデータに存在せず**、
   意味ジャンルは IOP Wiki 単源 `inferred`（source-backed はデフォルト衣装70件のみ）
10. **公式X（@EXILIUMJP）紐付け**: スキン行61本のうち37本(60.7%)を【スキン紹介】投稿と
    完全一致で紐付け（wikiru攻略wiki埋め込みリンク→fxtwitter API で実在確認の経路）。
    誤紐付け防止のため確証のない24本は空欄
11. **HTMLビューアの失敗連鎖**（教訓として記録）:
    - v1 表形式（temp保存）→ 「確認のしようがない」＋Desktop複製がゴミ扱い → 削除
    - v2 画像付き → IOP Wiki 画像が **Cloudflare hotlink遮断**で非表示
    - v3 画像149枚をローカル保存(186MB)してオフライン化 → それでも「UIしか出ない」
    - **根本原因: JS起動即クラッシュ（`ROWS.map is not a function`）**。
      埋め込みJSONがオブジェクト形式なのに対し配列としてアクセスしており、
      カード生成が最初から死んでいた。画像系の仮説はすべて外れ
    - v4 修正後、**監査スクリプト `audit-render.mjs`**（Headless Chrome 実描画検証・
      画像147/147読込・エラーゼロで PASS）を作ってから返したが、
      武田さん判断は「品質良くない、自分で探す」で**不採用**
12. ハルシネーション対策の運用が確立: ジャンル判定は2源照合（ゲームデータ＋IOP Wiki）のみ、
    不明は空欄、URL推測禁止・全URL実測確認、成果物は実描画監査を通してから提示

### Step 1 完了との整合（並行セッション分・2026-08-24 14:20時点）

- 並行セッションが **Step 1 完了**: ドライバ `10_extract_char.py`・決定性
  （canonical manifest SHA 方式確定）・機械突合12項目PASS・replay否定試験11系統PASS、
  いずれも **Helen/HelenSSR01** で実測（[[gf2-char-extract-handoff]] 参照）
- 本セッションの命名規則（`<Char><SSR/SR>01<スキン番号>`）とパイロット実績 HelenSSR01 は
  **整合**。HelenSSR0101（スキン1）は材質欠損の既知限界があり、基本衣装側が先に通ったのは
  台帳の材質数（Helen mat=8）とも矛盾しない
- 台帳の「名前ヒット≠完全在庫」限界は Step 1 の実装（cab-bundle-index による CAB解決追加）で
  一部改善されている。本ページの table.json の bundle_hits は旧走査値なので
  **抽出可否の最終判定には Step 1 のドライバ結果を優先**すること

### 再開点（別セッション用）

1. **パイロット2体の指名は未決**（武田さんが自分でスキンを探す方針に変更）。
   指名が固まれば `10_extract_char.py --char <族>` → `20_diff_char_blend.py` → 目視承認
2. 本ページの成果物: `wiki/_attachments/gf2-skin-genre-map/`（table.json=146行・
   viewer.html=不採用だが参照可・img/=186MB・audit-render.mjs=描画監査）
3. 資料の基準（何を抽出するかの優先度原理）は**未確定**。武田さんの序列感覚
   （服装最上位・立体感の把握・手持ちスキル差分）は手掛かりとして残す
4. SkinType 5テーマ（モダン/イリュージョン/クラシック/ノスタルジック/スタイリッシュ）と
   ClothesDuty 21クラスの語彙は復元済み。個別割当フィールドは未発見

## 未決事項（2026-08-24 夕方時点）

1. **パイロット候補の決定** — 未決。武田さんが自力でスキンを探す方針に変更
2. 資料の基準（優先度原理）の確定 — 未決
3. ビューアの再挑戦要否 — 不採用のままでよいか武田さん判断待ち
4. ox 利用期限までにパイロット目視まで通す（合意済みの仮案）

## 変遷

- 2026-08-24: 作成。選定相談セッションの記録。成果物（table.json/report.md）を temp から
  wiki/_attachments へ複製
- 2026-08-24（夕）: セッション再開分を追記。Table bytes 解読・公式X紐付け・ビューア失敗と
  監査スクリプト・Step 1 完了との整合・再開点を記録。ビューアは武田さん不採用
