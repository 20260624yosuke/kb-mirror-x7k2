---
type: analysis
status: active
confidence: high
evidence_level: source-backed
last_reviewed: 2026-08-30
sources:
  - project-current-state-page-plan-20260830
  - project-current-state-page-plan-review-20260830
---

# 実装計画 rev.2 の独立レビュー結果（2026-08-30・2回目）

対象: `wiki/builds/project-current-state-page-plan-20260830.md`（rev.2）
1回目のレビュー結果: `wiki/analyses/project-current-state-page-plan-review-20260830.md`
レビュー実施: 独立サブエージェント（Opus 5・読み取りのみ）。
**reasoning effort を指定する手段がメインエージェント側に無く、武田さんの「medium 以外禁止」を
満たせている保証は作れていない**（1回目と同じ制約）。指示に評価の誘導は入れていない。

## 総評

- 1回目の critical 5件のうち、**完全に解消したのは2件**（B-1 案2の復活 / B-6 自己ロック）。
- 残り3件（B-3 / B-4 / B-8）は**部分解消**。
- **rev.2 の改訂そのものが critical 2件を新たに生んだ**（A-1 / A-6）。
- **rev.2 で新しく書いた 1.2 の表に事実誤りがあった**（A-7）。メインエージェント側で実測して確認済み。

## 1回目の指摘の解消判定

| # | 指摘 | 判定 | 残っている問題 |
| --- | --- | --- | --- |
| B-1 | 承認済み「案2」の欠落 | **解消** | ただし A-2 の二重管理を新たに生んだ |
| B-3 | `run-state.json` 直読みによる誤読 | 部分解消 | 判定⑥を新設したが、除外規則・適用範囲・Bash 経由の検出が未定義 |
| B-4 | 判定②の入力未定義 | 部分解消 | 節8の入力欄が無い／節3の生成元が未定義／6節が読む7枚リストの出所が未定義 |
| B-6 | 判定⑤の自己ロック | **解消** | ただし A-1（判定⑤の無力化）を代わりに生んだ |
| B-8 | 本番合格が主観で通る | 部分解消 | 正解表を作る手段が存在しない（A-6） |
| B-2 | 壊れ#4 のすり替え | 解消 | ただし前提が事実誤認（A-7） |
| B-5 | `blocked` 語彙の同居 | **解消** | 別ファイル化で消滅 |
| B-7 | 判定④の検証・節5書式 | 部分解消 | ②の中身照合が `verify` 頼みで、その `verify` が未定義 |
| B-9 | 禁止事項が機械で守れない | 部分解消 | **8-4（LLM 区画の鮮度）は未解決のまま合格できる** |
| B-10 | `synced_from` スカラー等 | ほぼ解消 | `common.updated_at` を読む判定が1つも無い（前回指摘が残存） |
| B-11 | index/log/Inbox・frontmatter 承認 | **解消** | — |
| 追加1・2・5 | JSON 書き戻し／同時編集／`.bak` | **解消** | 別ファイル方式で消滅 |
| 追加3 | 未マウント時 | **解消** | 5.1-5 で「止める」 |
| 追加4 | フックのグローバル巻き添え | 部分解消 | 判定⑥に同等の限定が無い |
| 追加6 | 判定①の走査範囲 | **解消** | — |

## rev.2 で新しく生じた critical（2件）

### A-1 判定⑤が「壊れ#1」を検知できなくなった

- 該当: 4.4 末尾「判定⑤は共通欄ファイルの mtime だけを見る。既存 `run-state.json` の時刻は使わない」
  ＋ 5.1-1（`run-state.json` への書き込みは判定⑤の対象外）。
- ブレストメモの承認済み完成条件4-⑤は「**`run-state.json` と現在位置ページの時刻がずれていたら
  書き込みを止める**」。rev.2 の設計では、作業 LLM が `run-state.json` を更新しても判定⑤は発火せず、
  共通欄ファイルの mtime は動かないので `synced_from` とも一致したまま。
  **実データが進み、共通欄ファイルと現在位置ページだけが古い状態が、機械的に一切検出されない。**
- これは 1.1#1 の壊れそのもの。rev.2 はその再発を止められない。10節にも記載が無い。
- 観測: `06_repro-v51/run-state.json` は 2026-08-26 20:55 で停止、
  `wiki/builds/gf2-helen-repro-plan-repair-model-routing-handoff-20260827.md` は 2026-08-30 15:56 更新。
  **ずれは現在も進行中。**

### A-6 `verify` の仕様が存在せず、本番試験を開始できない

- 5.3 と 10節-1 が「(7.2)」を参照するが、**7節に 7.1/7.2 という小節は無い**。
  `verify` が何を照合するのかが計画のどこにも定義されていない。
- 8.2-1 は「`verify` 出力から工程・止まっている理由・**次の選択肢**を実データ由来で抽出して正解表を作る」
  とするが、節4「次の選択肢」は **LLM が書く区画**で、4.2 のスキーマに対応欄が無い
  （`next_action` は単数の1文）。**正解表を作れない＝承認Cの前提が成立しない。**

## rev.2 で新しく生じた major

- **A-2** 「現行/旧」が二重管理。6節のポインタブロック（手書き）と 4.2 の
  `related.handoffs[].status`（機械）が同じ情報を持ち、同期も一致検査も無い。
  11節で自ら却下した「必ず片方が古くなる」構造と同じ。
- **A-3** 節8「片付いた記録」の生成元が 4.2 に無い。判定②（共通欄から再生成）と判定③（ページ内で移動）が
  同じ節8を二重に所有し、どちらが勝つかの規定が無い。
- **A-4** 節3の生成元・所有権が未定義。8.1 試験7 は「その行を誰が書いたか」が決まらないと実施できない。
- **A-5** 6節の判定が読む「7枚のリスト」の出所が未定義。ハードコードすれば 11節で却下した
  「対応表案」と同じ構造に戻る。7枚のうち2枚は KB 外にあり、判定①の走査範囲とも別。
- **A-8** **Bash 経由スクリプト実行の誤検知が試験でカバーされていない。**
  2026-08-30 に実測された誤検知は「Bash で呼び出したスクリプト（`tools/inbox.py`）自体を
  書き込み対象と誤判定する」型。8.1 の試験2・3・4 は直接の書き込み試験だけで、この経路を試していない。
  helen の作業は `06_repro-v51/scripts/` の 258本を Bash で走らせて `logs/` `ledger/` へ書く形が主体。
  **実測**: `~/.claude/settings.json` の PreToolUse は matcher `Write|Edit|NotebookEdit|Bash` の1本のみ。
- **A-9** 手順11（残り3案件への展開）に承認関所が無い。KB CLAUDE.md 1A は
  「承認は対象群・入力条件の範囲内でのみ有効」「性質の違う対象への展開」を高リスクと定めている。

## A-7 rev.2 の 1.2 表の事実誤り（メインエージェントが実測で確認済み）

| 項目 | 計画 rev.2 の記載 | 実測（2026-08-30） |
| --- | --- | --- |
| #3 の行数 | 570 | **585** |
| 合計行数 | 4,578 | **4,593** |
| #1・#2 の「現行/旧の自己申告」 | 無し | **有り** |

**より重要な発見**: 壊れ#4 の実態は「印が無い」ではなく、**4枚が互いに「自分が正本」と主張している**こと。

- `gf2-helen-repro-v51-run.md` 14行目: 「…に従う実行の**正本**。この会話の記憶ではなく、
  このページと下記ファイル群が現在位置を持つ」
- `gf2-helen-repro-v51-handoff.md` 30〜31行目: 「前の版は `reports/HANDOFF.md`。
  **衝突したらこちらが新しい**」「版の正本は `run-state.json` の…」
- `gf2-helen-repro-plan-repair-model-routing-handoff-20260827.md` 54行目:
  「現行の提案・再開地点はsection 9を**正本とする**」
- `gf2-repro-and-swimsuit-conversation-handoff-20260827.md` 26行目: 「巨大な既存**正本**を置き換えず…」

したがって完成条件5（7枚すべてに現行/旧の印）は、**冒頭に「旧」ブロックを足しても本文が「正本」と
主張し続ける自己矛盾ページを4枚作るだけ**で達成扱いになる。しかも 9節-6 が本文の書き換えを禁じている
ため、**完成条件5・6節と禁止事項9-6が両立しない。**

## 10節に足すべきリスク（レビュー側の指摘）

1. 判定⑤が `run-state.json` のずれを見ない設計（A-1）＝最大の穴。
2. Bash 経由スクリプト実行の誤検知（A-8）。実測済みの症状なのに未記載。
3. 判定⑥の適用範囲・除外規則が未定義。
4. 「現行/旧」の二重管理（A-2）。
5. 現在位置ページが**8枚目の引き継ぎ資料**として既存7枚と併存する期間の混乱。
   7枚をいつ役目終了にするかが計画に無い。
6. 絶対パスがボリューム名 `/Volumes/SSD_M.2_Realtek RTL9210 NVME Media_/` に固定される。
   マウント名が変わると全案件が一斉に壊れる。10節-7 は未マウントのみを扱っている。
7. **水着化の作業ルートが KB 内（`output/gf2-helen-swimsuit/`）にあり、現在も活発に書き込まれている**
   （実測: 2026-08-30 16:30 / 16:31 更新）。ここを `work_roots` に入れると、
   判定⑤⑥が KB 内の進行中作業を直撃する。
8. KB CLAUDE.md 1A（高リスク成果物の自動判定・`quality-gate.json`）に該当するか否かの記載が無い。
   `gf2-helen-starlit-waltz/quality-gate.json` と `tools/project_quality_gate.py` は実在する。

## 問題なしと判定された箇所

- 4.1「別ファイル方式で何が解消されるか」の表4項目とも妥当。追加リスク1・2・5 は実際に消える。
- 5.1 の除外・限定・脱出口・警告化。**自己ロック（B-6）は確実に解けている**（A-1 の副作用付き）。
- 3.2 の `synced_from` 対応表化と所有者の明示。
- 手順の順序修正（フック登録→本番試験）と、承認A-0/A-1/B/C の停止規定。
- 8.1 の試験2・3・4（誤検知を検出するという発想）は正しい応答（A-8 の経路が抜けているだけ）。
- 1.1・1.4・4.4 の実測値は**すべて実ファイルと一致**。1.2 の表を除き、捏造・記憶書きの形跡は無い。

## この結果の扱い

**rev.2 も現状のままでは実装に渡せない。**
①判定⑤が形だけになり（A-1）、②本番試験を開始できない（A-6）。
さらに A-7 により、完成条件5 の中身そのものを設計し直す必要がある
（「印を足す」では解けず、本文の書き換えを許すか否かの判断が要る＝武田さんの決定事項）。
