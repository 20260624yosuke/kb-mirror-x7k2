---
type: analysis
status: active
confidence: high
evidence_level: source-backed
last_reviewed: 2026-08-31
sources:
  - project-current-state-page-plan-20260830
  - project-current-state-page-plan-review4-20260831
---

# 実装計画 rev.5 の独立レビュー結果（2026-08-31・5回目）

対象: `wiki/builds/project-current-state-page-plan-20260830.md`（rev.5・558行）
レビュー実施: 独立サブエージェント（Opus 5・読み取りのみ・ツール30回）。
**reasoning effort を指定する手段がメインエージェント側に無い**（5回とも同じ制約）。

## 総評

- **実測値は 1件を除きすべて実ファイルと一致**（行数・バイト数・フックの本数と matcher・103行・4,593行）。
- 4回目の critical 4件は **2件解消・2件は部分解消**。
- **より重い問題**: 2回目レビューの critical「A-1」が rev.5 でも未解消のまま残り、11節にも記載が無い。
- helen 分離の方針は正しいが、**切れ目に穴が3つ**。
- 重大度: **critical 4 / major 9 / minor 8**。

## critical（4件）

### A-1（2回目からの持ち越し・未解消）判定⑤が `run-state.json` のずれを見ない

- ブレストメモの**カード承認済み完成条件4-⑤**は
  「**`run-state.json` と現在位置ページの時刻がずれていたら書き込みを止める**」。
  rev.5 の判定⑤は共通欄ファイルの mtime しか見ず、**これを実装していない**。
- rev.5 の 1.1 表は壊れ#1 を「本計画の担当」としているが、作業 LLM が `run-state.json` を進めても
  共通欄ファイルが動かなければ判定は発火しない。**壊れ#1 の再発を止める手段が計画内に無い。**
- 4.2 に `source_files` を定義しているのに、**5節のどの判定も 8節の `verify` のどの項目も読まない**
  （grep で222行の1か所のみ）。定義だけあって消費者がいない。

### A-2 判定④が helen 分離により対象ゼロで、試験も本番も成立しない

- 完成条件4「6つの判定が動く」は判定④（案2ポインタの検査）を含む。対象の出所は
  `common.related.handoffs[]` のみで、helen では既存7枚を指す。
- **7枚へのポインタ挿入は14節へ分離したので、本計画では実施しない。** よって手順6の `check` は必ず FAIL。
  9.1 試験13 も、ポインタが1つも存在しないので実施できない。代替の試験用ファイルの規定も無い。
- **完成条件5「案2の仕組みが動く」は、適用対象ゼロで何をもって「動く」とするかが未定義。**

### A-3 判定④-③「網羅性」が循環定義で実装不能

- 6節「対象の出所は `related.handoffs[]` のみ」かつ判定④-③「`related.handoffs[]` が引き継ぎ資料を
  網羅していること」。**正解表が `handoffs[]` 自身しかないので恒真になる。**
  2回目レビュー A-5 が形を変えて残っている。

### A-4 承認済み決定の撤回が、承認事項として扱われていない

- メモ258行は**カード承認＋確認カード**で
  「**案1を採る＝全案件の `run-state.json` に共通の最小欄を足す**」を決めており、
  メモ479行には「**新しく小さい正本ファイルを作る案 → 却下**」がある。
- rev.5 の 4.1 は正反対に新規ファイル `run-state.common.json` 方式を採る。技術的理由は妥当だが、
  **これは承認済み決定の撤回**であり、KB CLAUDE.md 1A に照らして新たな承認が要る。
- rev.4 では 4.1 に「承認済み内容からの差分・承認A-0 の対象」と明示していた。
  **rev.5 でその枠を落とし、12節の「捨てた案」へ格下げした。**
  3.3 の節構成変更は「承認A-1 の対象」と正しく明示しているのに、4.1 には同じ扱いが無い。

## major（主なもの）

- **B-1** 判定⑤⑥の除外に **`06_repro-v51/logs/`（243エントリ）** と、相手の**実際の作業場**
  **`.audit-bootstrap-20260830/`** が無い。`06_repro-v51/audit/` は**まだ存在しない**（実測）。
  **除外リストは存在しないパスを守り、実在する作業場を守っていない。**
- **B-2** `work_roots` の**具体値がどこにも定義されていない**。除外・適用範囲・出力先・試験1〜7 と15 が
  すべてこの値に依存する。値が決まらないと除外が正しいかも判定できない。
- **B-3** D-2 の対処が自分のポインタ文と矛盾。6節のポインタ本文は
  「**現在位置**は … が入口」で抽出語を含む。マーカーの**境界行**自体も
  `name="現在位置"` を含み、内側か外側かが未定義。見出し `## 現在位置` も同様。
- **B-4** 手順3（4案件へ common 作成・関所なし）と承認C（残り3案件へ展開）が**二重計上**。
  11節-8 は「水着化を入れるかは承認C で判断」としているのに、手順3 が先に作る。
- **B-5** 壊れ#4 の残存検査が、**定義だけ本計画に残り、実装手順・試験・完成条件が無い**。
- **B-6** **Bash 経路の書き込み先推定は原理的に近似**。`brainstorm_guard.py` 79行に
  「完全な解析はできない前提の近似」と明記され、`WRITE_HINT` はリダイレクト系しか見ない。
  helen の主経路 `python3 scripts/f152.py` は**コマンド文字列から書き込み先が見えない**。
  → 9.1 試験15 は「検出できないから通る」だけで、誤検知試験として無効。
  同時に、判定⑤⑥ が helen の実運用を捕捉できないことも意味する。
- **B-7** グローバルフック登録（手順7）に**承認関所が無い**。登録先は `~/.claude/settings.json` で
  **マシン全体の全プロジェクトに効く**。KB CLAUDE.md 2節「スコープ拡大: 常駐処理」に該当する。
- **B-8** KB CLAUDE.md 1A（高リスク成果物・`quality-gate.json`）への**該当判定が無い**。
  10節-1 が「既存の規約に従う」と宣言しているのに、判定を書いていないのは自己矛盾。
- **B-9** 承認済みの判定⑤の差し替えと判定⑥の新設が、承認事項として書かれていない。
- **E-1**（F-1 の再発）水着化の更新時刻。計画は「2026-08-30 21:41」だが、
  **実測では 2026-08-31 に 66ファイルが更新され、最新は 10:00**。
  `tools/` 側でも swimsuit 関連スクリプトが 08-31 に6本以上更新されている。
  **別セッションが KB 内の `output/` と `tools/` を、今この時間帯に書いている。**
  rev.5 は `last_reviewed: 2026-08-31` でありながら 08-30 の値を載せており、
  4回目レビュー F-1 の批判（実測値を手打ちしない）がそのまま当てはまる。

## minor（主なもの）

- 3.3 が「手順2の雛形」と書くが、雛形は**手順1**（承認A-0 追加で番号がずれたまま）。
- 4.3 が判定⑤を「判定⑥（ずれ検知）」と誤記。
- **判定①の実装方式が未定義。** `current_state_page: true` は**この計画書自身**（3.2 のコードフェンス）や
  各レビューページにも文字列として存在するため、grep 実装だと**常時 FAIL する**。
  frontmatter パース限定であることを明記する必要がある。
- `<KBルート>/logs/` の新設が、実測で既に稼働している **`tools/logs/`**（`prose-guard.log` 等）と重複する。
- `answer-key` の出力先が KB 直下の `.md` で、`index.md` 未登録＝lint の「孤立ページ」との関係が未記載。
- `project_status` に消費者が無い。`list` / `resync --force` に実装手順も試験も無い。
- `state` 語彙5種を検証する手順が無く、**綴り誤りが黙って節3側に落ちる**。

## 完成条件の判定可能性

| 完成条件 | 判定 |
| --- | --- |
| 1 ページが存在 | 可能 |
| 2 8見出し＋マーカー | 可能 |
| 3 共通欄ファイル＋既存不変 | 可能（試験14）。ただし A-4 の承認問題が残る |
| 4 6つの判定が動く | **不可**（判定④は対象ゼロ・判定⑤は承認済み内容と別物） |
| 5 案2の仕組みが動く | **不可**（適用対象ゼロ・網羅性が循環） |
| 6 KB の整合性が崩れていない | **判定手順が9節に1つも無い** |
| 総合基準（9.2） | 可能。ただし節6・節7 の踏破を許すため、**当初の問題が解消したか区別できない**（11節に未記載） |

## 「KB の整合性を崩さない」は守れるか

- **守れている点**: `project_state_sync.py` は新規で、相手が変更予定のファイルと衝突しない。
  `prose_guard.py` の適用範囲は実読で `wiki/_attachments/**/*.html` と
  `wiki/analyses/brainstorm/**/*.md` のみ。`wiki/builds/*-current-state.md` は対象外なので、
  現在位置ページの生成が既存フックに止められることはない。既存フックを書き換えない方針も守られている。
- **守れていない／未検証**: B-1（相手の実作業場が除外に無い）／B-7（マシン全体へ3本目の deny を
  承認なしで追加）／B-6（Bash 経路の判定が近似）／B-8（`quality-gate.json` 規約への該当判定が無い）／
  E-1（KB 内で稼働中の別セッションの実態を古い時刻で把握している）。

## 問題なしと判定された箇所

- 3.4 の切り詰めラダー＋`verify` #8＋試験17（D-5 への筋の通った対処）。
- 5.1-2 の適用範囲限定、5.1-3 の脱出口、5.1-4 の警告化、5.1-5 の未マウント時停止。
- 手順順序（フック登録→本番試験）と承認記号の振り直し。
- 12節の「捨てた案」が rev.4 からの撤回2件を明示していて追跡可能な点。
- **14節の分離そのもの**（実行順序の合意・実パスと行数・禁止事項）は、受け取る側が動ける粒度。
  14.3 の行数は実測と完全一致。
- 15節の参照ファイルは**全件実在**。切れリンクは無い。

## この結果の扱い

**rev.5 も現状のままでは実装に渡せない。** とくに A-1（承認済み判定の未実装）・A-2/A-3（分離の切れ目で
完成条件4・5 が判定不能）・A-4（承認済み決定の撤回が承認関所を通っていない）は、
**計画の言葉を直すだけでなく、承認の取り直しが要る。**
