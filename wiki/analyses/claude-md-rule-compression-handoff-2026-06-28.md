---
type: analysis
status: active
confidence: high
evidence_level: inferred
last_reviewed: 2026-06-28
sources: []
---

# CLAUDE.md ふるまい規則の優先順位化 ― 経緯記録 + Codex 引き継ぎ

## この資料の目的

- 2026-06-28 に Claude(Opus 4.8)が `CLAUDE.md` のふるまい規則を圧縮・優先順位化した**経緯の記録**。
- Codex が wiki 内を点検し、整合のための**修正点候補**を探すための**引き継ぎ**。
- **Codex は候補を提示するに留め、構造変更は武田さんの承認を得てから**(`CLAUDE.md` 原則 #2「自律の境界」)。

## きっかけ(2次情報・相談ログ由来)

- 武田さんが Qiita 記事「正直に言う。お前の Claude Code の使い方は間違っている」(tehito, `raw/正直に言う。お前のClaude Codeの使い方は間違っている.md`)を読み、「ミス1: CLAUDE.md を全部入りの聖書にしている」が当 KB に当たるかを相談。
- 検証結果:当 KB の `CLAUDE.md`(479行)は前半・約170行がふるまい規則で、「(重要)」節が **8個**・重複あり・**順位なし**。本会話でも実害が出た(相談モードの武田さんへ作業の二択を2回押し売りし、却下された。「むやみに聞くな」と「構造は必ず確認しろ」が両方"重要"・順位なしで衝突)。

## 何をしたか(source-backed ― 実ファイル・差分で確認可能)

`CLAUDE.md` 前半のふるまい規則(旧8節 +「質問より宣言」+「正本優先」)を、`## ふるまいの優先順位` 配下の**優先順位つき6原則**へ統合した。

- **#1 段階を読む**(相談/計画/レビュー/実行/検証)― 最優先。**相談モードでは作業計画・成果物・二択を押し売りせず会話に留まる**を明文化し、**#2 より上位**と宣言。
- #2 自律の境界 / #3 説明の質(意味を省略しない) / #4 目的を取り違えない / #5 正本はファイル / #6 GUI・実機確認。
- 冒頭に再肥大の歯止め:「規則を足す前に順位付けで済まないか疑う / "重要"は数本に保つ / 半年に一度棚卸し」。
- **floor(安全規則・精度スキーマ)は一字も変えていない**:AI精度優先・共通入口/legacyガード・2次情報・ディレクトリ責務〜編集の原則(212行以降)は**バイト単位で不変**(バックアップとの diff で確認済み)。
- サイズ **479 → 400 行**(33.3KB → 28.5KB)。
- バックアップ(旧版): `~/.claude/plans_archive/CLAUDE.md.pre-rules-compress-20260628.bak`
- 計画正本(rev2): `~/.claude/plans/attach-dynamic-bee.md`

### 旧 → 新 対応表(無言削除ゼロの証明・Codex の過圧縮チェック用)

| 旧節(削除) | 統合先 |
|---|---|
| 開発実行中の対話・報告 | #2(報告の絞り込み)+ #1 |
| ユーザー向け説明: 意味を省略しない | #3 |
| 方針決定とユーザー確認 | #2 |
| 作業範囲の拡大防止 | #2(スコープ拡大・pilot)+ #1レビュー(問題3分類) |
| 目的の取り違え防止 | #4 + #1検証(実現性の区別) |
| 作業段階ごとのケア(計画/レビュー/実行/検証) | #1 |
| 前面 GUI・手動確認の選択提示 | #6 |
| 質問より宣言 | #1/#2(opt-out)+ #3(横文字注釈・**重複につき統合**) |
| 正本優先 | #5 |

そのまま残した floor: AI精度優先 / 共通入口・legacyガード / 2次情報 / ディレクトリ責務〜編集の原則(212行以降全部)。

## Codex に見てほしい修正点候補(inferred ― 提示のみ、適用は武田さん承認後)

1. **AGENTS.md の同期(2026-06-28 実施済み)**:`AGENTS.md`(Codex の入口)は、Codex 固有の操作規則を残したうえで、前半のふるまい規則を `CLAUDE.md` と同じ6原則へ同期済み。旧状態は「Claude は新6原則 / Codex は旧8節」で食い違っていた。
2. **llm-wiki skill の陳腐化**:`~/.claude/skills/llm-wiki/`(SKILL.md / reference.md)は初代の汎用版で、`builds/`・精度スキーマ(`evidence_level` 等)・Canvas/映像/transcript サブモードを**持たない**(`CLAUDE.md` が先行)。候補:skill を現行へ同期、または「意図的に汎用テンプレ」と明記して食い違いを既知化。
3. **CLAUDE.md 操作節のポインタ化(deferred = option 2)**:操作手順(236–367 行付近)は今もインライン。skill を同期した後なら「詳細は専用 skill 参照」へ畳める。
4. **新6原則の過圧縮チェック**:上の対応表と実ファイル `CLAUDE.md` を突き合わせ、落ちた規則がないか/#1 が重すぎないかをレビュー。

## Codex 実行結果(2026-06-28)

- `AGENTS.md` の旧8節・重複していた「質問より宣言」・末尾の重複した「正本優先」を、`## ふるまいの優先順位` の6原則へ統合した。
- `AGENTS.md` 後半の Wiki 運用規則、`~/.agents/skills/llm-wiki/` 参照、Codex では映像 ingest を実行しない規則は維持した。
- `CLAUDE.md` は今回編集していない。
- 未着手: `llm-wiki` skill の現行仕様同期、`CLAUDE.md` 操作節のポインタ化、新6原則の長期運用観察。

## 適用しない方針(今回の合意)

- 規則を**足して**直さない。「数本の鋭い優先順位に絞り、あとは削る」が本 KB の合意。Codex も同じ方針で候補を出す(再肥大させない)。
- 「相談か作業か」を読む判断は**ファイルだけで完結しない**(必要だが十分でない)。実証は今後のセッション + 武田さんの「まず話だけ / 実行していい」の一言での即補正。

## 関連リンク

- [[llm-maintainer-handoff-plan]] ― LLM maintainer 引き継ぎ計画の正本
- [[projects-dashboard]] ― 進行中プロジェクト一覧
- メモリ: `feedback_conversation_mode_default`(相談モードで作業を押し売りしない)
